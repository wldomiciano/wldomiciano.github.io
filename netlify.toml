[build]
  publish = "public"
  command = "hugo --gc --minify --cleanDestinationDir"

[context.production.environment]
  HUGO_VERSION = "0.84.3"
  HUGO_ENV = "production"
  HUGO_ENABLEGITINFO = "true"
